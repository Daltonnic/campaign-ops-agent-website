# CLAUDE.md

Repository-specific instructions for Claude.

## Git workflow

After pushing changes to a feature branch, **always**:

1. Open a pull request against `master`.
2. Then ask the user whether they want it merged.

Do not merge without explicit approval, but do not wait to be asked to open the PR — that step is the default.
